<div class="panel panel-black" style="margin-top: -3%; background-color: #1c2a48;">
    <div class="panel-heading" >
<i><b><h1 style="color: white; text-align: center;">Index</h1></b></i>
    </div>
    <div class="panel-body">
    <div class="well">
<i>
<table class="table table-hover table-responsive">
    <thead>
      <ul>
        <tr>
          <th>price</th>
          <th>name</th>
          <th>description:</th>
            <th>image:</th>
          <th>show:</th>
          <th>edit:</th>
       <th>delete:</th>
         
         
        </tr>
      </thead>

  <%# byebug %>
  <% @products.each do |product| %>
      <tr>
         <td><%= product.price %></td>
        <td><%= product.name %></td>
        <td><%= product.description %></td>
        <!-- fetch of image code -->
     <td><% product.images.each do |images| %>
        <%= image_tag images.image, :class => 'img-responsive', :style => 'height:80px' %>
<% end %>
</td>
     <td><%= link_to 'Show', product %></td>
     <td><%= link_to 'Edit', edit_product_path(product) %></td>
     <td><%= link_to 'Destroy', product, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    </ul>
    <% end %>

  </thead>
</table>
 <td><%= link_to 'New ', new_product_path, :class => 'btn btn-primary '%></td>
 </i>
</div>
</div>
</div>